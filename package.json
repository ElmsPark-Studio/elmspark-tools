{
  "name": "elmspark-playwright-tests",
  "version": "1.0.0",
  "description": "End-to-end UI verification tests for ElmsPark plugins on dev.elmspark.com. Run BEFORE shipping any plugin admin / JS / form change.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
